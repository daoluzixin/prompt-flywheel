🙋 Round 1  2026-04-17 22:31:02
用户：1. /Users/user_a/code/service_xxx_datasync/src/main/java/com/example_corp/deliverydriver/tcr/datasync/domain/service/sync/SyncBaseService.java 2. /Users/user_a/code/service_xxx_datasync/src/main/java/com/example_corp/deliverydriver/tcr/datasync/adaptor/TcResourceBaseTableRepository.java 3. /Users/user_a/code/service_xxx_datasync/src/main/java/com/example_corp/deliverydriver/tcr/datasync/domain/service/DBCacheSyncService.java 4. /Users/user_a/code/service_xxx_datasync/src/main/java/com/example_corp/deliverydriver/tcr/datasync/domain/dtssync/module/BaseDtsDtsSyncHandler.java 5. /Users/user_a/code/service_xxx_datasync/src/main/java/com/example_corp/deliverydriver/tcr/datasync/domain/dtssync/DtsSyncCacheDispatcher.java 6. /Users/user_a/code/service_xxx_datasync/src/main/java/com/example_corp/deliverydriver/tcr/datasync/app/invoker/DataSyncInvokerImpl.java 7. /Users/user_a/code/service_xxx_datasync/src/main/java/com/example_corp/deliverydriver/tcr/datasync/app/DBCacheTravelSyncApp.java 返回所有文件的完整代码内容，重点关注 createCacheByDB 方法、如何构建缓存、以及如何使用 DataItem。***
AI：以下是7个文件的完整代码内容及其分析：  ---  ## 1. SyncBaseService.java  package com.example.delivery.tcr.datasync.domain.service.sync; import com.example.delivery.tcr.cache.resource.TCResourceCacheKvService; import com.example.delivery.tcr.cache.vo.CacheObjRawWrapper; import com.example.delivery.tcr.cache.vo.TCResourceBaseCache; import com.example.delivery.xxx.common.util.TCResourceCacheUtils; import com.example.delivery.xxx.common.vo.TCResourceBaseAttribute; import com.example.delivery.tcr.datasync.adaptor.TcResourceBaseTableRepository; import com.example.delivery.tcr.datasync.domain.service.DBCacheSyncService; import com.example.delivery.tcr.datasync.vo.BaseKey; import com.example.delivery.tcr.datasync.vo.DBSyncContext; import com.example.platform.biz.cache.exception.BizKvException; import org.springframework.stereotype.Service; import javax.annotation.Resource; import java.io.IOException; /** - @ClassName SyncBaseService - @Description base同步缓存服务 - @Author dev_a - @Date 2021/12/1 */ @Service public class SyncBaseService extends DBCacheSyncService<BaseKey, TCResourceBaseAttribute, TCResourceBaseCache> { @Resource private TCResourceCacheKvService tcResourceCacheKvService; @Resource private TcResourceBaseTableRepository baseTableRepository; @Override protected CacheObjRawWrapper<TCResourceBaseCache> getCache(BaseKey key) throws BizKvException { return tcResourceCacheKvService.getBaseFromMaster(key.getEntityId()); } @Override protected TCResourceBaseAttribute getDB(BaseKey key) { return baseTableRepository.queryBaseTableFromDb(key.getEntityId()); } @Override protected TCResourceBaseCache createCacheByDB(DBSyncContext ctx, TCResourceBaseAttribute db) throws IOException { return TCResourceCacheUtils.buildTransportCapacityBaseCache(db); } @Override protected boolean casCache(DBSyncContext ctx, TCResourceBaseCache newCache, CacheObjRawWrapper<TCResourceBaseCache> originCache) throws Exception { return tcResourceCacheKvService.casBase(ctx.getEntityId(), originCache.getOrigin(), newCache); } }  **分析**： - 继承自 `DBCacheSyncService<BaseKey, TCResourceBaseAttribute, TCResourceBaseCache>` - **`createCacheByDB` 方法**：直接调用 `TCResourceCacheUtils.buildTransportCapacityBaseCache(db)` 构建缓存对象 - 使用了 `TCResourceBaseAttribute` 作为 DB实体类型，`TCResourceBaseCache` 作为缓存实体类型  ---  ## 2. TcResourceBaseTableRepository.java  package com.example.delivery.tcr.datasync.adaptor; import com.example.delivery.xxx.common.service.TransportCapacityBaseAttributeService; import com.example.delivery.xxx.common.vo.TCResourceBaseAttribute; import com.example.delivery.tcr.datasync.util.RateLimitAop; import lombok.extern.slf4j.Slf4j; import org.springframework.stereotype.Component; import javax.annotation.Resource; /** - @program: TcResourceBaseTableRepository - @description: - @author: dev_b - @create: 2021-10-24 **/ @Component @Slf4j public class TcResourceBaseTableRepository { @Resource TransportCapacityBaseAttributeService baseAttributeService; @RateLimitAop.RateLimit(entrance = "readDB") public TCResourceBaseAttribute queryBaseTableFromDb(Long entityId) { return baseAttributeService.getTransportCapacityBaseAttribute(entityId); } }  **分析**： - 简单的 DB 查询适配器 - 通过 `TransportCapacityBaseAttributeService`查询 `TCResourceBaseAttribute` - 使用 `@RateLimitAop.RateLimit` 注解进行限流保护  ---  ## 3. DBCacheSyncService.java（核心抽象类）  package com.example.delivery.tcr.datasync.domain.service; import com.alibaba.fastjson.JSON; import com.example.dp.zebra.group.router.ZebraForceMasterHelper; import com.google.common.collect.Sets; import com.example.mdp.boot.starter.config.annotation.MdpConfig; import com.example.delivery.tcr.cache.vo.CacheObjRawWrapper; import com.example.delivery.xxx.common.vo.TCResourceAttribute; import com.example.delivery.tcr.datasync.constant.DBCacheSyncResultEnum; import com.example.delivery.tcr.datasync.util.CatX; import com.example.delivery.tcr.datasync.vo.BaseKey; import com.example.delivery.tcr.datasync.vo.DBSyncContext; import com.example.platform.biz.cache.exception.BizKvException; import com.example.platform.biz.degrade.utils.BizDegradeUtils; import lombok.extern.slf4j.Slf4j; import org.apache.commons.lang.BooleanUtils; import org.springframework.util.StringUtils; import java.io.IOException; import java.util.Set; import static com.example.delivery.tcr.datasync.constant.DBCacheSyncResultEnum.DB_NOT_EXISTS; import static com.example.delivery.tcr.datasync.constant.DegradeKey.*;* *import static com.example.delivery.tcr.datasync.constant.MccKey.RELATIVE_DB_SYNC_GAP_SECS;* *import static com.example.delivery.tcr.datasync.util.CatX.TagK.*; /** - @ClassName DBCacheSyncService - @Description db缓存同步 - @Author dev_a - @Date 2021/12/1 */ @Slf4j public abstract class DBCacheSyncService<Key extends BaseKey, DbEntity extends TCResourceAttribute, CacheEntity> { @MdpConfig(RELATIVE_DB_SYNC_GAP_SECS + ":1") private int relativeDBGapTimeInSecs; protected DBSyncContext createContext(Long entityId) { return DBSyncContext.builder().entityId(entityId).build(); } protected CatX createCatX(DBSyncContext ctx, Key key) { return CatX.init() .setMetricName("db_cache_sync_" + (ctx.getMetricNameSuffix() == null ? "" : ctx.getMetricNameSuffix())) .tag(CatX.TagK.SUBJECT, getClass().getSimpleName()) .tag(PHARSE, "handle") .objectId(key.getEntityId()); } protected void preCheck(DBSyncContext ctx, DbEntity dbEntity, CacheObjRawWrapper<CacheEntity> cacheEntity) throws IllegalArgumentException { if (dbEntity == null) { throw new IllegalArgumentException("dbEntity is null"); } } protected abstract CacheObjRawWrapper<CacheEntity> getCache(Key key) throws BizKvException; protected abstract DbEntity getDB(Key key); protected DbEntity getDBFromMaster(Key key) throws BizKvException { try { ZebraForceMasterHelper.forceMasterInLocalContext(); return getDB(key); } catch (Exception e) { throw e; } finally { ZebraForceMasterHelper.clearLocalContext(); } } public DBCacheSyncResultEnum handle(DBSyncContext context, Key key) { DBSyncContext ctx = context == null ? createContext(key.getEntityId()) : context; CatX catX = createCatX(ctx, key); ctx.setCatX(catX); try { if (isSyncDegrade()) { catX.tag(STATUS, DBCacheSyncResultEnum.CAS_DEGRADE.name()).reportCount(1); return DBCacheSyncResultEnum.CAS_DEGRADE; } CacheObjRawWrapper<CacheEntity> cache = getCache(key); DbEntity db = getDBFromMaster(key); return syncCacheDB(ctx, db, cache); } catch (Exception e) { log.error("AbstractDBCacheSyncService.handle#ex. ctx:{} key:{}", ctx, key, e); catX.tag(STATUS, "exception").reportCount(1).log("发生异常:" + e.getMessage(), key); return DBCacheSyncResultEnum.EXCEPTION; } } protected boolean ignoreWhenDBNull() { return true; } private DBCacheSyncResultEnum syncCacheDB(DBSyncContext ctx, DbEntity dbEntity, CacheObjRawWrapper<CacheEntity> originCache) throws Exception { CatX catX = ctx.getCatX().objectId(ctx.getEntityId()); if (ignoreWhenDBNull() && dbEntity == null) { catX.tag(STATUS, DB_NOT_EXISTS.name()).reportCount(1).log(DB_NOT_EXISTS.getMsg(), null); return DB_NOT_EXISTS; } //ensure db entity preCheck(ctx, dbEntity, originCache); //compare if need cas boolean needCas = this.needFix(ctx, dbEntity, originCache == null ? null : originCache.getObj()); if (BooleanUtils.isFalse(needCas)) { catX.tag(STATUS, DBCacheSyncResultEnum.NORMAL.name()).reportCount(1).log(DBCacheSyncResultEnum.NORMAL.getMsg(), null); return DBCacheSyncResultEnum.NORMAL; } catX.tag("detail", (ctx.getCacheVer() > ctx.getDbVer()) ? "cache-greater" : "cache-lower"); //cas operation degrade if (this.isCasCacheFromDBDegrade()) { catX.tag(STATUS, DBCacheSyncResultEnum.DO_CAS_DEGRADE.name()).reportCount(1) .log(DBCacheSyncResultEnum.DO_CAS_DEGRADE.getMsg(), null); return DBCacheSyncResultEnum.DO_CAS_DEGRADE; } //create cache from db CacheEntity newCache = createCacheByDB(ctx, dbEntity); //pre cas check if (BizDegradeUtils.isNotDegrade(SYNC_CACHE_CREATED_CACHE_FIELDS_NOT_MATCH_EXISTS) && !preCheckCasCache(ctx, newCache, originCache)) { log.error("AbstractDBCacheSyncService.syncCacheDB#preCheckCasCache false. class:{} newCache:{} originCache:{}", getClass().getSimpleName(), newCache, originCache); catX.tag(STATUS, DBCacheSyncResultEnum.CREATED_CACHE_FIELDS_NOT_MATCH_ORIGIN.name()).reportCount(1) .log(DBCacheSyncResultEnum.CREATED_CACHE_FIELDS_NOT_MATCH_ORIGIN.getMsg(), newCache); return DBCacheSyncResultEnum.CREATED_CACHE_FIELDS_NOT_MATCH_ORIGIN; } //cas boolean casSuccess = this.casCache(ctx, newCache, originCache); if (casSuccess) { catX.tag(STATUS, DBCacheSyncResultEnum.CAS_SUCCESS.name()).reportCount(1).log("cas执行成功", newCache); return DBCacheSyncResultEnum.CAS_SUCCESS; } else { catX.tag(STATUS, DBCacheSyncResultEnum.CAS_FAIL.name()).reportCount(1).log("cas执行失败", newCache); log.error("DTS cas 缓存({}) cas fail DBEntity:{} newCache:{} originCache:{}", getClass().getSimpleName(), dbEntity, newCache, originCache); return DBCacheSyncResultEnum.CAS_FAIL; } } protected boolean preCheckCasCache(DBSyncContext ctx, CacheEntity newCache, CacheObjRawWrapper<CacheEntity> originCache) { if (originCache == null || StringUtils.isEmpty(originCache.getOrigin())) { return true; } Set<String> newCacheKeys = JSON.parseObject(JSON.toJSONString(newCache)).keySet(); Set<String> originCacheKeys = JSON.parseObject(originCache.getOrigin()).keySet(); return Sets.symmetricDifference(newCacheKeys, originCacheKeys).isEmpty(); } protected abstract CacheEntity createCacheByDB(DBSyncContext ctx, DbEntity db) throws IOException; /** - 是否降级 * - @return */ protected boolean isSyncDegrade() { return BizDegradeUtils.isDegrade(String.format(SYNC_CACHE_DEGRADE_PREFIX, getClass().getSimpleName())); } /** - 比较缓存数据是否需要更新 * - @param dbEntity - @param cacheEntity * @return */ protected boolean needFix(DBSyncContext ctx, DbEntity dbEntity, CacheEntity cacheEntity) { //cache not exists CatX catX = ctx.getCatX().copyByMetricName().tag(PHARSE, "needFix"); if (cacheEntity == null) { catX.tag(SUBJECT, "no-cache").tag(STATUS, "true").reportCount(1); return true; } //relatice db gap time if (dbEntity.getUtime() < 1L) { catX.tag(SUBJECT, "db-no-utime").tag(STATUS, "false").reportCount(1); log.error("DBCacheSyncService.needFix#db-no-utime. dbEntity:{} cacheEntity:{}", dbEntity, cacheEntity); return false; } if (System.currentTimeMillis() / 1000 - dbEntity.getUtime() < relativeDBGapTimeInSecs) { catX.tag(SUBJECT, "gap-utime").tag(STATUS, "false").reportCount(1); return false; } //compare version if not equals Long dbVer = dbEntity.getVersion(); Long cacheVer = JSON.parseObject(JSON.toJSONString(cacheEntity)).getLong("version"); if (dbVer == null || cacheVer == null) { log.error("DBCacheSyncService.needCasCacheFix#no version. class:{} dbVer:{} cacheVer:{}", getClass().getSimpleName(), dbVer, cacheVer); catX.tag(SUBJECT, "no-version").tag(STATUS, "false").reportCount(1); return false; } ctx.setCacheVer(cacheVer); ctx.setDbVer(dbVer); return dbVer.longValue() != cacheVer.longValue(); } /** - 实际执行 cas 是否降级, 降级后将不执行 cas 操作，仅统计不一致情况 * - @return */ protected boolean isCasCacheFromDBDegrade() { return BizDegradeUtils.isDegrade(String.format(SYNC_CACHE_EXECUTE_CAS_DEGRADE_PREFIX, getClass().getSimpleName())); } /** - cas刷新缓存 * - @return 是否写入成功 */ protected abstract boolean casCache(DBSyncContext ctx, CacheEntity newCache, CacheObjRawWrapper<CacheEntity> originCache) throws Exception; }  **核心分析**：  这是一个**模板方法模式**的抽象类，定义了 DB 与缓存同步的核心流程：  ###核心方法  1. **`handle()` - 主入口方法**： - 检查降级开关 - 获取缓存 `getCache(key)` - 从主库获取 DB 数据 `getDBFromMaster(key)` - 调用 `syncCacheDB()` 执行同步  2. **`syncCacheDB()` - 核心同步逻辑**： - DB 为空检查 - `preCheck()` 预检查 - `needFix()` 判断是否需要更新（通过 version 比较） - **`createCacheByDB()`** - 从 DB 实体构建缓存对象 - `preCheckCasCache()` - 检查新旧缓存字段是否一致 - `casCache()` - 执行 CAS 写入缓存  3. **`needFix()` - 判断是否需要更新**： - 缓存不存在 →需要更新 - DB utime 时间检查（避免近期更新的数据） - **比较 version 字段**：`dbVer != cacheVer` → 需要更新  4. **`createCacheByDB()` - 抽象方法**： - 子类实现，将 DB 实体转换为缓存实体  ### DataItem 使用方式从代码可以看出，`DbEntity extends TCResourceAttribute`，这是数据库实体基类
